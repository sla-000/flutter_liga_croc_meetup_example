name: example_with_ns
description: example_with_ns
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  packet_a:
    path: ../packet_a
  packet_b:
    path: ../packet_b

  provider: ^5.0.0

  rxdart: ^0.26.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
