name: meetup_example_2
description: liga-croc-meetup-example
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  packet_a:
    path: ../packet_a
  packet_b:
    path: ../packet_b

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
