name: meetup_example
description: liga-croc-meetup-example
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  example_non_ns:
    path: ../example_non_ns
  example_with_ns:
    path: ../example_with_ns

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
