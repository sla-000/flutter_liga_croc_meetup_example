name: meetup_example
description: liga-croc-meetup-example
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  example_non_ns:
    path: ../example_non_ns
  example_with_ns:
    path: ../example_with_ns


dev_dependencies:
  flutter_test:
    sdk: flutter


dependency_overrides:
  freezed_annotation: ^0.12.0 # caused by sdk: ">=2.7.0 <3.0.0", if 2.12 replace with ^0.14.0
  built_value: ^8.0.0

flutter:
  uses-material-design: true
